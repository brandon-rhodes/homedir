#!/bin/bash

set -e

mkdir -p ~/.fonts
if [ ! -f ~/.fonts/unixpc-system.8.bdf ]
then
    wget -P ~/.fonts https://raw.githubusercontent.com/brandon-rhodes/unixpc-font-bdf/master/unixpc-system.8.bdf
fi
mkfontdir ~/.fonts
exec xterm \
     -fa '' \
     -font 'unix-pc-system-8' \
     -fb 'unix-pc-system-8' \
     -bg black \
     -fg green \
     -cm \
     +bdc \
     -xrm 'xterm*boldMode: off' \
     /bin/bash
