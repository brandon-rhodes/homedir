# When SSH cannot set up a -D, -L, or -R port-forward, I would rather
# that it fail and return me to the command line than for it to show me
# that it succeeded in getting a prompt.

ExitOnForwardFailure yes

# Hosts included here get the benefit of tab completion at the zsh
# and bash command lines.

Host kenaniah
  ForwardAgent yes
  ForwardX11 yes

Host asaph
  HostName asaph.rhodesmill.org
  ForwardAgent yes

Host wf-rhodesmill
  User rhodesmill
  HostName 108.59.11.69

Host bitbucket.org
  User hg

Host jackies-imac
